.login-container {
  min-height: 100vh;
  width: 100%;
  display: flex;
  align-items: stretch; /* garante que colunas ocupem toda a altura */
  justify-content: flex-start;
  position: relative;
  overflow: hidden;
  padding-top: 0; /* body.no-navbar-padding will handle navbar padding */
}

// Lado esquerdo - Branding e informações
.login-left {
  flex: 1 1 50%;
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 50%, #1e40af 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  position: relative;
  overflow: hidden;

  &::after {
    content: '© 2026 - Todos os direitos reservados.';
    position: absolute;
    bottom: 1rem;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.8rem;
    font-weight: 500;
  }

  .brand-section {
    max-width: 500px;
    color: white;
    text-align: center;
    position: relative;
    z-index: 2;

    .logo-wrapper {
      margin-bottom: 0.5rem;

      .brand-logo {
        width: 250px;
        height: auto;
        fill: white;
      }
    }

    .brand-description {
      margin-bottom: 3rem;

      h2 {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        line-height: 1.2;
      }

      p {
        font-size: 1.125rem;
        line-height: 1.6;
        opacity: 0.9;
      }
    }

    .features-list {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      margin-bottom: 3rem;

      .feature-item {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
        text-align: left;
          padding: 1rem;
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.05);
          backdrop-filter: blur(10px);
          border: 1px solid rgba(255, 255, 255, 0.1);
          transition: all 0.3s ease;
          animation: fadeInUp 0.6s ease-out forwards;
          opacity: 0;
          transform: translateY(20px);

          &:nth-child(1) { animation-delay: 0.1s; }
          &:nth-child(2) { animation-delay: 0.2s; }
          &:nth-child(3) { animation-delay: 0.3s; }
          &:nth-child(4) { animation-delay: 0.4s; }

          &:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
          }

          .feature-icon {
            font-size: 2rem;
            flex-shrink: 0;
            margin-top: 0.25rem;
            color: #ffffff;
            transition: transform 0.3s ease;
          }

          &:hover .feature-icon {
            transform: scale(1.1);
          }

          .feature-text {
            h4 {
              font-size: 1.1rem;
              font-weight: 700;
              margin-bottom: 0.5rem;
              color: #ffffff;
            }

            p {
              font-size: 0.95rem;
              line-height: 1.4;
              opacity: 0.85;
              margin: 0;
              color: #f3f4f6;
      overflow: hidden;
      z-index: 1;

      .floating-element {
        position: absolute;
        font-size: 3rem;
        opacity: 0.1;
        animation: float 8s ease-in-out infinite;

        &.apple {
          top: 15%;
          left: 10%;
          animation-delay: 0s;
        }

        &.carrot {
          top: 25%;
          right: 20%;
          animation-delay: 2s;
        }

        &.broccoli {
          bottom: 30%;
          left: 20%;
          animation-delay: 4s;
        }

        &.banana {
          bottom: 20%;
          right: 15%;
          animation-delay: 1s;
        }

        &.orange {
          top: 70%;
          left: 75%;
          animation-delay: 3s;
        }
      }
    }
  }
}

// Lado direito - Formulário de login
.login-right {
  flex: 1 1 50%;
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(20px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1.5rem;
  min-width: 320px; /* permite melhor ajuste em larguras menores */
  overflow: hidden;
}

.login-card {
  width: 100%;
  max-width: 400px;
  background: transparent;
  border-radius: 0;
  box-shadow: none;
  padding: 0;
  animation: slideUp 0.6s ease-out;
  display: flex;
  flex-direction: column;
  margin: 0 auto;
}

.form-container {
  background: linear-gradient(135deg, rgba(37, 99, 235, 0.08) 0%, rgba(29, 78, 216, 0.05) 100%);
  border-radius: 16px;
  padding: 1.5rem;
  margin-bottom: 2rem;
  border: 1px solid rgba(37, 99, 235, 0.1);
  box-shadow: 0 4px 20px rgba(37, 99, 235, 0.08);
  backdrop-filter: blur(10px);
  max-height: calc(100vh - 200px);
  overflow: auto;
  animation: slideInRight 0.8s ease-out;
}

.login-header {
  text-align: center;
  margin-bottom: 2rem;

  .mobile-logo {
    display: none; // Oculto por padrão
    margin-bottom: 1.5rem;

    .brand-logo-mobile {
      width: 120px;
      height: auto;
      // filter: brightness(0) invert(1); // Removido para manter cores originais
      opacity: 0.9;
    }
  }

  .welcome-section {
    h2 {
      font-size: 1.75rem;
      font-weight: 700;
      color: #1f2937;
      margin-bottom: 0.5rem;
    }

    p {
      color: #6b7280;
      font-size: 1rem;
      line-height: 1.4;
    }
  }
}
.login-form {
  .form-group {
    margin-bottom: 1.25rem;

    label {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-weight: 600;
      color: #374151;
      margin-bottom: 0.5rem;
      font-size: 0.95rem;

      .icon {
        font-size: 1.1rem;
      }
    }

    input {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 2px solid #e5e7eb;
      border-radius: 12px;
      font-size: 1rem;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-sizing: border-box;
      background: #ffffff;

      &:focus {
        outline: none;
        border-color: #2563eb;
        box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.1);
        transform: translateY(-1px);
      }

      &.invalid {
        border-color: #ef4444;
        background: #fef2f2;

        &:focus {
          box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.1);
        }
      }

      &::placeholder {
        color: #9ca3af;
        font-style: italic;
      }
    }

    .input-with-icon {
      position: relative;

      .input-icon {
        position: absolute;
        left: 0.75rem;
        top: 50%;
        transform: translateY(-50%);
        color: #6b7280;
        font-size: 1.1rem;
        pointer-events: none;
      }

      input {
        padding-left: 2.5rem;
        padding-right: 2.5rem; // espaço para o ícone direito se houver
      }

      .icon-right {
        position: absolute;
        right: 0.75rem;
        top: 50%;
        transform: translateY(-50%);
        background: none;
        border: none;
        color: #6b7280;
        cursor: pointer;
        font-size: 1.1rem;
        padding: 0.25rem;
        border-radius: 4px;
        transition: color 0.2s;

        &:hover {
          color: #2563eb;
        }
      }
    }

    .error-message {
      color: #dc2626;
      font-size: 0.85rem;
      margin-top: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.25rem;
      font-weight: 500;

      &::before {
        content: '⚠️';
        font-size: 0.8rem;
      }
    }
  }

  .form-group-checkbox {
    margin-bottom: 1.5rem;
    display: flex;
    justify-content: flex-start;
    align-items: center;

    .checkbox-label {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
      user-select: none;
      font-size: 0.9rem;
      color: #374151;
      font-weight: 500;

      input[type="checkbox"] {
        width: 18px;
        height: 18px;
        cursor: pointer;
        accent-color: #2563eb;
        border-radius: 4px;
      }

      &:hover {
        color: #10b981;
      }
    }
  }

  .btn-login {
    width: 100%;
    padding: 1rem 1.25rem;
    background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    box-shadow: 0 6px 20px rgba(37, 99, 235, 0.4);
    position: relative;
    overflow: hidden;

    &::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s;
    }

    &:hover:not(:disabled) {
      transform: translateY(-3px) scale(1.02);
      box-shadow: 0 10px 30px rgba(37, 99, 235, 0.5);

      &::before {
        left: 100%;
      }
    }

    &:active:not(:disabled) {
      transform: translateY(-1px) scale(0.98);
      box-shadow: 0 4px 12px rgba(37, 99, 235, 0.4);
    }

    &:disabled {
      opacity: 0.7;
      cursor: not-allowed;
      transform: none;
      box-shadow: 0 2px 8px rgba(37, 99, 235, 0.2);
    }

    .spinner {
      width: 20px;
      height: 20px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      border-top-color: white;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
  }
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.hint-box {
  margin-top: 1.5rem;
  padding: 1rem;
  background: linear-gradient(135deg, rgba(37, 99, 235, 0.08) 0%, rgba(29, 78, 216, 0.05) 100%);
  border-radius: 12px;
  border: 1px solid rgba(37, 99, 235, 0.1);
  box-shadow: 0 4px 12px rgba(37, 99, 235, 0.08);

  h3 {
    font-size: 1rem;
    font-weight: 600;
    color: #1f2937;
    margin-bottom: 0.5rem;
  }

  p {
    font-size: 0.9rem;
    color: #6b7280;
    margin-bottom: 1rem;
    line-height: 1.4;
  }

  .chips {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;

    .chip {
      padding: 0.25rem 0.75rem;
      background: #2563eb;
      color: white;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 500;
      box-shadow: 0 2px 4px rgba(37, 99, 235, 0.2);
    }
  }
}

.login-footer {
  margin-top: 1.5rem;
  text-align: center;
  padding-top: 1.5rem;
  border-top: 1px solid #e5e7eb;

  p {
    color: #6b7280;
    font-size: 0.9rem;
    margin: 0.25rem 0;

    a {
      color: #2563eb;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.2s;

      &:hover {
        color: #1d4ed8;
        text-decoration: underline;
      }
    }
  }

  .footer-info {
    margin-top: 0.5rem;

    p {
      font-size: 0.8rem;
      opacity: 0.8;
    }
  }
}

// Responsividade
@media (max-width: 900px) {
  .login-container {
    flex-direction: column;
    align-items: stretch;
    justify-content: center;
    padding-top: 0;
    margin-top: 0;
  }

  .login-left {
    display: none;
  }

  .login-right {
    min-width: 0;
    width: 100%;
    padding: 2rem 1rem;
  }
}
@media (max-width: 1024px) {
  .login-left {
    flex: 0.8;
  }

  .login-right {
    flex: 1.2;
  }
}

@media (max-width: 768px) {
  .login-container {
    flex-direction: column;
    height: 100vh;
  }

  .login-left {
    display: none; // Oculta a seção de branding em telas menores
  }

  .login-right {
    flex: 1;
    min-height: 100svh; // Ocupa toda a altura disponível (safer on mobile)
    padding: 1.5rem 1rem;
    position: relative; // Para posicionar o footer relativamente
  }

  .login-card {
    max-width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center; // centraliza verticalmente o conteúdo do cartão
    align-items: stretch;
  }

  .login-header .mobile-logo {
    display: block; // Mostra o logo em telas menores
  }

  .form-container {
    padding: 1.5rem;
    border-radius: 12px;
    margin-bottom: 1.5rem;
    flex: 1; // Ocupa o espaço disponível
    overflow: auto; // permite rolar se o formulário for maior que a tela
    max-height: calc(100svh - 160px);
  }

  .login-footer {
    margin-top: auto; // Empurra o footer para o final
    margin-bottom: 1rem; // Espaço do final da tela
    position: relative;
    border-top: none;
    padding-top: 1rem;
  }

  .login-header .welcome-section h2 {
    font-size: 1.5rem;
  }

  .login-form .form-group-checkbox {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }
}

@media (max-width: 480px) {
  .login-left {
    padding: 1rem 0.75rem;

    .brand-section {
      .logo-wrapper {
        margin-bottom: 0.25rem;

        .brand-logo {
          width: 120px;
        }

      }

      .brand-description {
        margin-bottom: 1rem;

        h2 {
          font-size: 1.25rem;
        }

        p {
          font-size: 0.9rem;
        }
      }

      .features-list {
        gap: 0.75rem;

        .feature-item .feature-icon {
          font-size: 1.5rem;
        }

        .feature-text h4 {
          font-size: 1rem;
        }

        .feature-text p {
          font-size: 0.85rem;
        }
      }

      .nutrition-elements .floating-element {
        font-size: 1.5rem;
      }
    }
  }

  .login-right {
    padding: 1rem 0.75rem;
  }

  .login-header {
    margin-bottom: 1.25rem;

    .welcome-section h2 {
      font-size: 1.25rem;
    }

    .welcome-section p {
      font-size: 0.9rem;
    }
  }

  .login-form .form-group {
    margin-bottom: 1rem;

    input {
      padding: 0.75rem 1rem;
    }
  }

  .form-container {
    padding: 1.25rem;
    border-radius: 10px;
    margin-bottom: 1.5rem;
  }

  .login-form .form-group-checkbox {
    margin-bottom: 1.5rem;
  }

  .login-form .btn-login {
    padding: 0.75rem 1.25rem;
    font-size: 1rem;
  }

  .login-footer {
    margin-top: 1.5rem;
    padding-top: 1.5rem;

    p {
      font-size: 0.85rem;
    }
  }
}
